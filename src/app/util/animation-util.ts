/**
 * アニメーション関連の操作を提供するユーティリティクラスです。
 */
export class AnimationUtil {

    /**
     * スピナーを開始します。
     * @param スピナーを表示するDOM要素。ViewChild経由で取得されることを想定する。
     */
    public static startSpinner(spinner: HTMLInputElement): void {
        
    }
}
